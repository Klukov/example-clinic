spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/example-clinic
    username: test
    password: test123
logging:
  level:
    org:
      springframework:
        web: DEBUG
      hibernate:
        SQL: TRACE
